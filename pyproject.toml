[project]
name = "tulip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "caetanogenete@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "more-itertools>=11.1.0",
    "readchar>=4.2.2",
    "rich>=15.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pyright>=1.1.410",
    "pytest>=9.0.3",
    "ruff>=0.15.16",
]
